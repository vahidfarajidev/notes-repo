# آموزش کامل نصب و تنظیم Git برای VS Code و پروژه‌های PHP

در این مقاله، تمام نکات مربوط به نصب Git روی ویندوز، تنظیم آن برای VS Code، و تفاوت بین گزینه‌های مهم نصب آورده شده است.

---

## 🧩 ۱. نصب Git

### دانلود و نصب
1. به سایت رسمی بروید: [https://git-scm.com/downloads](https://git-scm.com/downloads)
2. نسخه Windows را دانلود و نصب کنید.
3. در هنگام نصب، به گزینه‌های زیر توجه کنید 👇

---

## ⚙️ تنظیمات مهم هنگام نصب Git

### **۱. Adjust the name of the initial branch**
Git از شما می‌پرسد نام شاخه‌ی اولیه هنگام ایجاد مخزن (`git init`) چه باشد.

✅ **پیشنهاد:** گزینه **Override the default branch name** را انتخاب کنید و در فیلد بنویسید:

```
main
```

> این کار باعث می‌شود شاخه‌ی پیش‌فرض پروژه شما با GitHub و Azure هماهنگ باشد.

---

### **۲. Adjust your PATH environment**
سه گزینه دارد:

| گزینه | توضیح | پیشنهاد |
|--------|--------|----------|
| Use Git from Git Bash only | فقط در Git Bash کار می‌کند | ❌ نه |
| Git from the command line and also from 3rd-party software | در PowerShell، CMD و VS Code قابل استفاده است | ✅ بله |
| Use Git and optional Unix tools from the Command Prompt | ابزارهای Unix را هم اضافه می‌کند (ممکن است تداخل کند) | ⚠️ فقط برای کاربران حرفه‌ای |

> پس گزینه دوم را انتخاب کنید تا VS Code بتواند Git را بشناسد.

---

### **۳. Choose the default behavior of `git pull`**

Git از شما می‌پرسد موقع اجرای `git pull` چطور شاخه‌ها را با هم ترکیب کند.

#### گزینه‌ها:

| گزینه | توضیح | پیشنهاد |
|--------|--------|----------|
| Fast-forward or merge | تغییرات را با merge اضافه می‌کند (تاریخچه حفظ می‌شود) | ✅ بهترین گزینه برای شروع |
| Rebase | تغییرات شما را روی آخرین نسخه بازنویسی می‌کند (تاریخچه تمیز ولی خطرناک‌تر) | ⚠️ برای حرفه‌ای‌ها |
| Only ever fast-forward | فقط اگر بدون conflict باشد، pull می‌کند | ❌ محدود |

---

## 🔍 تفاوت Merge و Rebase با تصویر ذهنی

فرض کنید شاخه‌ی `main` روی سرور تا commit `C` جلو رفته و شما دو commit محلی (`D`, `E`) دارید:

```
A --- B --- C   ← main (remote)
          \
           D --- E   ← شما (local)
```

### 🔹 حالت Merge
Git یک commit جدید برای ادغام می‌سازد:

```
A --- B --- C ----------- F   ← main
          \             /
           D --- E ----'   ← merge commit
```

✅ مزایا:
- تاریخچه واقعی حفظ می‌شود  
- امن‌تر برای کار تیمی  

❌ عیب:
- تاریخچه شاخه‌دارتر می‌شود

---

### 🔹 حالت Rebase
Git تغییرات شما را بر بالای آخرین نسخه remote قرار می‌دهد:

```
A --- B --- C --- D' --- E'   ← main (تاریخچه خطی)
```

✅ مزایا:
- تاریخچه خطی و تمیز  
❌ معایب:
- commitهای قبلی بازنویسی می‌شوند  
- در کار تیمی خطرناک‌تر است

---

### 📊 مقایسه سریع

| ویژگی | Merge | Rebase |
|--------|--------|---------|
| حفظ تاریخچه واقعی | ✅ بله | ❌ نه |
| تمیزی تاریخچه | ⚠️ شاخه‌دار | ✅ خطی و مرتب |
| مناسب برای تیم‌ها | ✅ بله | ⚠️ فقط حرفه‌ای‌ها |

---

## 🧠 نتیجه نهایی برای نصب Git

1. **Branch Name:** `main`  
2. **PATH Option:** `Git from the command line and also from 3rd-party software`  
3. **Pull Behavior:** `Fast-forward or merge`  

با این تنظیمات:
- Git در VS Code به‌درستی کار می‌کند  
- پروژه‌های PHP به‌راحتی روی GitHub یا Azure منتشر می‌شوند  
- خطای `git not recognized` برطرف می‌شود  

---

🧾 نویسنده: ChatGPT  
📅 نسخه: 2025  
📘 مناسب برای: VS Code + PHP + GitHub + Azure
